{
  "rules": {
    ".read": true,
    ".write": true,
    "targets": {
      "$target": {
        ".validate": "newData.isString() && newData.val().contains('..') === false"
      }
    },
    "votes": {
      "$name": {
        ".validate": "newData.hasChildren(['value', 'datetime'])",
        "value": {
          ".validate": "newData.isString()"
        },
        "datetime": {
          ".validate": "newData.isNumber()"
        }
      }
    }
  }
}
